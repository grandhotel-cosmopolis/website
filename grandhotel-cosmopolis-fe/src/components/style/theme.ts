import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#325432",
    },
    secondary: {
      main: "#735B4F",
    },
  },
});

export default theme;
